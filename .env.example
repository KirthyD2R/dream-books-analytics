# PostgreSQL connection string for the Dream Books database.
# The dashboard reads from this DB. For local dev you can point it at a
# copy/replica so analytics queries never touch production load.
DATABASE_URL="postgresql://user:password@localhost:5432/dreambooks?schema=public"
